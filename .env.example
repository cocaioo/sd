# ============================================================
# Arquivo de configuração para deployment distribuído
# ============================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para '.env'
# 2. Configure as variáveis abaixo conforme seu cenário
# 3. Execute: docker-compose -f docker-compose-client.yml up -d
#

# ====== CONFIGURAÇÃO DO SERVIDOR ======
# IP ou hostname do PC que está rodando o servidor RPC
# Exemplos:
#   - Rede local: RPC_SERVER_HOST=192.168.1.100
#   - Hostname: RPC_SERVER_HOST=pc-do-colega
#   - Localhost (teste): RPC_SERVER_HOST=localhost
RPC_SERVER_HOST=192.168.1.100

# Porta do servidor RPC (padrão: 8000)
RPC_SERVER_PORT=8000

# ====== CONFIGURAÇÃO DO MONITOR (OPCIONAL) ======
# URL do monitor para limpeza de logs quando cliente é attachado
# Deixe vazio para desabilitar notificação automática
# Exemplo: RPC_MONITOR_URL=http://192.168.1.100:5000/control/clear
RPC_MONITOR_URL=
